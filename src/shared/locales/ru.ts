import { HeaderTextModel } from '@/shared/locales/text-models'

const header = HeaderTextModel.ru

export const ru = {
  header: {
    language: header.language,
  },
}
export type LocaleType = typeof ru
